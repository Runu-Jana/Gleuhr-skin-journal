# Required in remote/cloud dev environments (Gitpod, Codespaces, tunnels, etc.)
# Disables webpack-dev-server host check that returns 403 when Host header != localhost
DANGEROUSLY_DISABLE_HOST_CHECK=true
# Makes WebSocket HMR use the same port as the page instead of hardcoding localhost:3000
WDS_SOCKET_PORT=0
